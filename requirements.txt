# ==============================================================================
# PYTORCH INSTALLATION - READ BEFORE INSTALLING
# ==============================================================================
# PyTorch must be installed FIRST based on your hardware:
#
# NVIDIA GPU:  pip3 install torch torchvision torchaudio
# AMD GPU:     pip3 install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/rocm6.1
# CPU only:    pip3 install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
#
# After PyTorch is installed, run: pip install -r requirements.txt
# ==============================================================================

# Core ML Frameworks (PyTorch installed separately - see above)
# torch>=2.0.0  # COMMENTED OUT - Install separately based on your hardware
transformers>=4.30.0
datasets>=2.14.0
pandas>=2.0.0
scikit-learn>=1.3.0
tqdm>=4.65.0
numpy>=1.24.0
matplotlib>=3.7.0
seaborn>=0.12.0
pyarrow>=12.0.0
mlflow>=2.9.0
optuna>=3.5.0
accelerate>=0.26.0